{
  "name": "mean",
  "version": "1.0.0",
  "description": "MEANingful project",
  "main": "app/index.js",
  "scripts": { 
    "test": "echo \"Yeah, no test specified\" && exit 0",
    "clean": "rm -rf ./dist",
    "copy": "cp package.json ./dist/package.json && cp app/* ./dist/app",
    "setup": "mkdir -p ./dist/app && npm run copy",
    "build": "npm run clean && npm run setup",
    "start": "npm run watch"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/RaketenWirt/first-mean.git"
  },
  "keywords": [
    "mean"
  ],
  "author": "DK",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/RaketenWirt/first-mean/issues"
  },
  "homepage": "https://github.com/RaketenWirt/first-mean",
  "dependencies": {
    "express": "^4.10.2",
    "socket.io": "^1.3.5"
  }
}
